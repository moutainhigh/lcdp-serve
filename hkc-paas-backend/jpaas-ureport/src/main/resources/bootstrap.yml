server:
  port: 7302



spring:
  application:
    name: jpaas-ureport

#        metadata:
#          version: redxun
  devtools:
    restart:
      #热部署生效
      enabled: true
        #设置重启的目录
        #additional-paths: src/main/java
      #classpath目录下的WEB-INF文件夹内容修改不重启
      exclude: WEB-INF/**
